# Core application dependencies.
# Install with:  pip install -r requirements.txt

asgiref==3.12.1
Django==6.0.7
dj-database-url>=2.3,<4.0
django-cors-headers==4.9.0
djangorestframework==3.17.1
djangorestframework_simplejwt==5.5.1
gunicorn>=23.0,<24.0
pillow==12.3.0
psycopg[binary]>=3.2,<4.0
PyJWT==2.13.0
python-decouple==3.8
sqlparse==0.5.5
stripe==11.4.1
whitenoise>=6.9,<7.0

# MySQL is optional. The project runs on SQLite out of the box, and
# mysqlclient needs MySQL development headers to build, which breaks
# installation on machines that do not have them.
# To use MySQL, install the headers for your system and then run:
#     pip install -r requirements-mysql.txt
